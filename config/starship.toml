format="$username$hostname$localip$shlvl$singularity$kubernetes$directory${custom.directories}${custom.files}$vcsh$fossil_branch$git_branch$git_commit$git_state$git_metrics${custom.giturl}$git_status$hg_branch$pijul_channel$docker_context$package$bun$c$cmake$cobol$daml$dart$deno$dotnet$elixir$elm$erlang$fennel$golang$gradle$haskell$haxe$helm$java$julia$kotlin$lua$nim$nodejs$ocaml$opa$perl$php$pulumi$purescript$python$raku$rlang$red$ruby$rust$scala$solidity$swift$terraform$vlang$vagrant$zig$buf$guix_shell$nix_shell$conda$meson$spack$memory_usage$aws$gcloud$openstack$azure$env_var$crystal$custom$sudo$cmd_duration$line_break$jobs$battery$time$status$container$os$shell$character"

[character]
# format = "$symbol "
success_symbol = "[❯](bold green)"
error_symbol = "[❯❯❯](bold red)"
# vimcmd_symbol = "[❮](bold green)"
# vimcmd_visual_symbol = "[❮](bold yellow)"
# vimcmd_replace_symbol = "[❮](bold purple)"
# vimcmd_replace_one_symbol = "[❮](bold purple)"
# disabled = false

[directory]
# truncation_length = 3
# truncate_to_repo = true
# fish_style_pwd_dir_length = 0
# use_logical_path = true
# format = "[$path]($style)[$read_only]($read_only_style) "
# repo_root_format = "[$before_root_path]($before_repo_root_style)[$repo_root]($repo_root_style)[$path]($style)[$read_only]($read_only_style) "
style = "blue bold"
# disabled = false
# read_only = "🔒"
# read_only_style = "red"
# truncation_symbol = ""
# home_symbol = "~"
# use_os_path_sep = true

[git_branch]
# format = "on [$symbol$branch(:$remote_branch)]($style) "
# symbol = " "
style = "green"
# truncation_length = 9223372036854775807
# truncation_symbol = "…"
# only_attached = false
# always_show_remote = false
# ignore_branches = []
# disabled = false

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "red"
# stashed = '\$'
# ahead = "⇡"
# behind = "⇣"
# up_to_date = ""
# diverged = "⇕"
# conflicted = "="
# deleted = "✘"
# renamed = "»"
# modified = "!"
# staged = "+"
# untracked = "?"
# typechanged = ""
# ignore_submodules = false
# disabled = false

[custom.giturl]
# disabled = true
description = "Display symbol for remote Git server"
command = """
GIT_REMOTE=$(command git ls-remote --get-url 2> /dev/null)
if [[ "$GIT_REMOTE" =~ "github" ]]; then
    GIT_REMOTE_SYMBOL=" github "
elif [[ "$GIT_REMOTE" =~ "git.frama-c.com" ]]; then
    GIT_REMOTE_SYMBOL=" frama-c "
elif [[ "$GIT_REMOTE" =~ "gitlab.mpi-sws.org" ]]; then
    GIT_REMOTE_SYMBOL=" mpi-svs "
elif [[ "$GIT_REMOTE" =~ "git.eleves.ens.fr" ]]; then
    GIT_REMOTE_SYMBOL=" ENS "
elif [[ "$GIT_REMOTE" =~ "gitlab" ]]; then
    GIT_REMOTE_SYMBOL=" gitlab "
elif [[ "$GIT_REMOTE" =~ "bitbucket" ]]; then
    GIT_REMOTE_SYMBOL=" "
elif [[ "$GIT_REMOTE" =~ "git" ]]; then
    GIT_REMOTE_SYMBOL=" "
else
    GIT_REMOTE_SYMBOL=" "
fi
echo "$GIT_REMOTE_SYMBOL "
"""
when = 'git rev-parse --is-inside-work-tree 2> /dev/null'
style = "bright-yellow bold"
format = "at [$output]($style) "

[custom.coq]
description = "The currently installed version of Coq"
command = "coqc --version | grep -Eo '[0-9]+\\.[0-9]+\\.[0-9]+' | head -n 1"
symbol = "🐔"
format = "via [$symbol( v$output )]($style) "
detect_extensions = [
    "v",
]
detect_files = [
    "_CoqProject",
]
style = "green bold"

[custom.directories]
description = "Show directory count count for current directory"
command = "find ./ -maxdepth 1 -type d | tail -n +2 | wc -l"
format = "📁[$output]($style) "
style = "blue"
when = "true"

[custom.files]
description = "Show files count for current directory"
command = "find ./ -maxdepth 1 -type f | wc -l"
format = "📝[$output]($style) "
style = "white"
when = "true"
